"""scratchlm: autoregressive language models built from scratch.

The package walks from classical text classification up to a GPT-style
transformer decoder, implementing each component (BPE tokenizer, n-gram model,
RNN/LSTM cells, self-attention, samplers) without high-level shortcuts.
"""

__version__ = "0.1.0"
